<h1>Report a Review</h1>

<%= simple_form_for [@report_review, @review] do |f|%>
  <% f.label :issue %>
  <%= f.input :issue, as: :select, collection: (['Inappropriate', 'Language', 'Irrelevant', 'Other']) %>

  <% f.label :comment %>
  <%= f.input :comment, as: :text %>

  <%= f.button :submit, class: "button" %>
<% end %>
